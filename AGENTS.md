# AGENTS.md

## Repo status
- **No code yet.** This is a specification/planning-phase repo for an online voting system.
- The README defines the domain model: users, parties, candidates, votaciones (elections), votes, and audit snapshots.

## Key files
- `README.md` — full project specification (domain model, states, rules)
- `docs/plan/plan-template.md` — planning template
- `docs/spec/spec-template.md` — spec template

## Conventions
- Templates use Markdown with emoji section headers and tables. Follow that style when creating new docs.
- `.gitignore` excludes `.idea/` and `*template.md` (templates themselves are tracked, but copies named `*-template.md` are ignored).

## No build/lint/test
- There is no code, no `package.json`, no build system. All files are Markdown documentation.
